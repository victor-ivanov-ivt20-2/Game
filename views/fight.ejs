<!DOCTYPE html>
<html lang="ru">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <meta http-equiv="X-UA-Compatible" content="ie=edge">
  <link rel="stylesheet" href="../static/css/style.css">
  <link rel="stylesheet" href="../static/css/fight.css">
  <title>В путь</title>
  <style>
  #myProgress {
    width: 100%;
    background-color: #ff0000;
  }

  #myBar {
    width: 1%;
    height: 30px;
    background-color: #ddd;
  }
  </style>
</head>
<body>
  <header class="header">
    <div class="container">
      <div class="header__inner">
        <nav class="menu">
          <ul class="menu__list">
            <li class="menu__list-item">Здоровье = <span id="health"></span></li>
            <li class="menu__list-item">Здоровье противника = <span id="e_health"></span></li>
            <li class="menu__list-item">кусок хлеба = <span id="small_heal"></span></li>
            <li class="menu__list-item">кумыс = <span id="big_heal"></span></li>
          </ul>
        </nav>
        <a href="/main_menu">
          Сбежать
        </a>
      </div>
    </div>
  </header>
  <div class="container">
    <h1 class="level"><b>Уровень : </b><span id="fight__id"><%= fightID %></span></h1>
    <div class="enemy">
      <img src="../static/resources/images/unknown__1.png" alt="" id="enemy__image" class="enemy__png">
    </div>
    <button onclick="health = sattack(health);">сильный удар</button>
    <button onclick="health = attack(health);">удар</button>
    <button onclick="health = heal(health);">выпить зелье</button>
    <button onclick="health = bigheal(health);">выпить большое зелье</button>
    <div id="myProgress">
      <div id="myBar"></div>
    </div>
    <br>
    <button onclick="move(health)">Click Me</button>

  </div>
  <script type="text/javascript">
  var i = 0;
function move(health) {
  if (true) {
    var elem = document.getElementById("myBar");
    var width = 1;
    width += health;
    elem.style.width = width + "%";
    return width;
    // var id = setInterval(frame, 10);
    function frame() {
      if (width >= 100) {
        clearInterval(id);
        i = 0;
      } else {
        width++;
        elem.style.width = width + "%";
      }
    }
  }
}
  </script>
  <script src="../static/js/figth.js" charset="utf-8"></script>
</body>
</html>
